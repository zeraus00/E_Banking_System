{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "AllowedHosts": "*",

    "ConnectionStrings": {
        // This is the default connection string for SQL Server
        // It connects to the local SQL Server instance and uses the master database.
        // You can change the database name to your desired database.
        "DefaultConnection": "Server=localhost;Database=E_BankingSystem;Trusted_Connection=True;TrustServerCertificate=True;"
    }
}
